---
export interface Props {
	champion: string;
}

const { champion } = Astro.props;
const splashImage = `http://ddragon.leagueoflegends.com/cdn/img/champion/splash/${champion}_0.jpg`;
---

<img src={splashImage} alt="A League of Legends character">

